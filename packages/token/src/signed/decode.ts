/**
 * Decode a signed compact token without verification (payload only).
 * @internal Placeholder for implementation.
 */
export function decodeSigned(_token: string): {
  header: unknown;
  payload: unknown;
} {
  throw new Error(
    'decodeSigned is not yet implemented. Coming in a future release.',
  );
}
