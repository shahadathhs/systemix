/**
 * Decode and verify a signed compact token.
 * @internal Placeholder for implementation.
 */
export function verifySigned<T = unknown>(_token: string, _secret: string): T {
  throw new Error(
    'verifySigned is not yet implemented. Coming in a future release.',
  );
}
